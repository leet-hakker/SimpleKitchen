//! Yes!
#![feature(lint_reasons)]

/// Module containing logic and types related to the `ingredient` table
pub mod ingredient;
/// Module containing logic and types related to the `recipe` table
pub mod recipe;
/// Module containing logic and types related to the `recipe_ingredient` table
pub mod recipe_ingredient;
/// Module containing implementation of custom types used within the database
pub mod types;
/// Module containing logic and types related to the `user` table
pub mod user;
